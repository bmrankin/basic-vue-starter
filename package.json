{
  "name": "basic-vue",
  "version": "0.1.0",
  "description": "Basic Vue Starter",
  "main": "app.js",
  "scripts": {
    "start": "npm run browsersync",
    "test": "echo \"Error: no test specified\" && exit 1",
    "browsersync": "browser-sync start -s -f *.html, *.js"

  },
  "author": "@bmrankin",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.7"
  }
}
